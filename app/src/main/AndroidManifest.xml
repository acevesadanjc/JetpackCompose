<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".ComposeHiltApp"
        android:resizeableActivity="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:targetApi="31">

        <activity
            android:name=".SplashActivity"
            android:configChanges="screenSize|smallestScreenSize|orientation|screenLayout|uiMode"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Splash.Theme"
            android:windowSoftInputMode="adjustResize"
            tools:ignore="LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.ui.activity.LoginActivity"
            android:exported="false"
            android:label="@string/title_activity_login"
            android:theme="@style/Splash.Theme"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.features.lemonapp.diceRoller.DiceRollerActivity"
            android:exported="false"
            android:label="@string/app_name_dice_roller"
            android:theme="@style/Splash.Theme"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.features.lemonapp.LemonadeActivity"
            android:exported="false"
            android:label="@string/app_name_lemonade"
            android:theme="@style/Splash.Theme"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.features.tipcalculatorap.TipCalculatorActivity"
            android:exported="false"
            android:label="@string/app_name_tip_time"
            android:theme="@style/Splash.Theme"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".ui.features.artspace.ArtSpaceActivity"
            android:exported="false"
            android:label="@string/app_name_tip_time"
            android:theme="@style/Splash.Theme"
            tools:ignore="LockedOrientationActivity" />
    </application>

</manifest>